package com.hungh2002.config.environmentVariable;

public class Env {

    // Database configuration
    //// relational database management system
    public static final String RDBMS = "mysql";
    public static final String HOST = "localhost";
    public static final int PORT_OF_HOST = 3306;
    //////
    /////// database name
    // public static final String DATABASE_NAME = "online_shopping_website";
    //////// database name --> used for testing
    public static final String DATABASE_NAME = "online_shopping_website_test";
    //////
    public static final String USERNAME = "hungh2002";
    public static final String PASSWORD = "1905";
    //// Create table script
    public static final String CREATE_PRODUCTS_TABLE_SCRIPT =
            "CREATE TABLE IF NOT EXISTS `products` (`product_id` bigint(20) NOT NULL AUTO_INCREMENT,`name` varchar(100) NOT NULL,`category` varchar(100) NOT NULL,`price` decimal(13,4) DEFAULT NULL,`image` varchar(100) NOT NULL,`create_at` timestamp NOT NULL DEFAULT current_timestamp(),PRIMARY KEY (`product_id`)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci";
    public static final String CREATE_CUSTOMER_TABLE_SCRIPT =
            "CREATE TABLE IF NOT EXISTS `customers` ( `customer_id` bigint(20) NOT NULL AUTO_INCREMENT, `username` varchar(100) NOT NULL, `password` varchar(100) NOT NULL, `session_id` varchar(100) DEFAULT NULL, PRIMARY KEY (`customer_id`), UNIQUE KEY `customers_unique` (`username`) ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci";
    public static final String CREATE_CART_TABLE_SCRIPT =
            " CREATE TABLE IF NOT EXISTS `cart` (`cart_id` bigint(20) NOT NULL AUTO_INCREMENT,`product_id` bigint(20) NOT NULL,`customer_id` bigint(20) NOT NULL,`quantity` bigint(20) NOT NULL,PRIMARY KEY (`cart_id`),KEY `cart_customers_FK` (`customer_id`),KEY `cart_products_FK` (`product_id`),CONSTRAINT `cart_customers_FK` FOREIGN KEY (`customer_id`) REFERENCES `customers` (`customer_id`),CONSTRAINT `cart_products_FK` FOREIGN KEY (`product_id`) REFERENCES `products` (`product_id`)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci";
    public static final String CREATE_SELLERS_TABLE_SCRIPT =
            " CREATE TABLE IF NOT EXISTS `sellers` (`seller_id` bigint(20) NOT NULL AUTO_INCREMENT,`product_id` bigint(20) NOT NULL,`customer_id` bigint(20) NOT NULL,`quantity` bigint(20) NOT NULL,PRIMARY KEY (`seller_id`),KEY `seller_customers_FK` (`customer_id`),KEY `seller_products_FK` (`product_id`),CONSTRAINT `seller_customers_FK` FOREIGN KEY (`customer_id`) REFERENCES `customers` (`customer_id`),CONSTRAINT `seller_products_FK` FOREIGN KEY (`product_id`) REFERENCES `products` (`product_id`)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci";

    // Base URL
    public static final String BASE_URL = "http://127.0.0.1:8080";

}
